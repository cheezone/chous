# Project without server directory

This project demonstrates that `strict` rules do not require directories to exist.
The `server` directory is optional, and the strict rule only applies if it exists.
